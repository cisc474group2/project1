let length=int(Math.random*10)%3+3;
let sequence=[];
let setSequence=function(){
    let i=0;
    for(i;i<length;i++){
        sequence[i]=int(Math.random*10)%4;
    }
};
let playSequence=function(){
    let seqPos=0;
    while(seqPos<length){
        
    }
}